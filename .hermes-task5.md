You are the OpenCode executor for Phase B Task 5 only. Do not start Task 6 (no Add traveller in this task).

Work from: /Users/evolvex/Desktop/Visatop-next-js
Branch: feat/tourist-journey-phase-b

## Hard rules
- Types `T*`, interfaces `I*` (not `type I*`), arrow functions, `FC` + `I*Props` on new components
- Do NOT commit
- Do NOT git stash
- Do NOT invent hex colors — use existing Tailwind / CSS variables (`border-primary`, `bg-card`, `rounded-[12px]`, ClientButton, ClientField)
- Do NOT classify visas from `name` or duration in the UI — only `filterGuidedServices` / `visibleStayBuckets`
- Do NOT say “party”
- Do NOT POST `travelers[]` yet (Task 6). Keep create body as `{ nationalityCode, serviceId, catalogCurrency, guestEmail }`
- Fetch `GET /api/catalog/apply-config` for badge strings. Missing fetch → use `DEFAULT_APPLY_PRICE_BADGES` from `lib/apply/apply-config.ts`
- Extend `Service` type with `stayBucket`, `entryKind`, `travelerKind`, `showInGuidedChooser`

## Task 5: Replace the card wall with the question flow

### Create `lib/apply/apply-stay-labels.ts`

```typescript
export const APPLY_STAY_LABELS: Record<TStayBucket, string> = {
  "1_14": "1–14 days",
  "15_30": "15–30 days",
  "31_60": "31–60 days",
  transit: "Transit",
  "5_year": "5 years",
};
```

These five labels only. Do not invent extra buckets.

### Create `components/apply/all-in-price-badges.tsx`

`FC` + `IAllInPriceBadgesProps { badges: TApplyPriceBadges }`. Render the two strings from props (Task 2 settings). Not hardcoded JSX.

### Create `components/apply/guided-visa-chooser.tsx`

After nationality is known (parent already has `services` + `nationality`):

1. Stay buttons = `visibleStayBuckets(services)` only, labeled via `APPLY_STAY_LABELS`.
2. Entry: Single / Multiple — **skip** when stay is `transit`, or every remaining row for that stay is `entryKind === "either"`, or only one entry kind exists among remaining rows (treat `either` as matching both; if after stay filter every row is `either` OR all share one of `single`/`multiple`, skip).
3. Primary traveler: Adult / Child — hide Child if no child SKU in the current stay filter (`travelerKind === "child"` among chooser-visible rows for that stay).
4. Matching priced rows only via `filterGuidedServices`. CTA **Continue with this visa** sets the selected `serviceId` (call `onSelectService(id)`). Parent still owns submit.
5. Email stays on the parent form (already required).
6. Do not reimplement USD/AED — parent already has the currency toggle.

Empty shortlist exact copy: **No visa matches these answers. Change your answers or contact us.**

Do not invent a product.

Reuse existing card chrome for the matching shortlist (duration/name/price) so it still looks like Visatop, but only filtered rows.

### Modify `components/apply/start-application-form.tsx`

- Replace the “Visa type” 10-card wall (`services.map` grid) with `<GuidedVisaChooser />`.
- Fetch apply-config once (with nationalities or services load) and pass `badges` into `AllInPriceBadges` near the price/currency section.
- Keep email + Next submit as today (`serviceId` required).
- Previous button is currently `type="submit"` with `onClick` to `/` — change Previous to `type="button"` so it does not submit.
- Loading: keep `ClientServiceCardsSkeleton` while services load.
- Services with all `stayBucket` null / opted out: chooser stay list empty → show the empty shortlist sentence (do not fall back to the old unfiltered wall).

### Verify

```bash
pnpm exec vitest run lib/apply/guided-visa-filter.test.ts
```

No new classifier tests needed. Print file list.
