import { AdminAuthLoadingSkeleton } from "@/components/ui/app-loading";

/**
 * Loading placeholder for client sign-in / sign-up (two-column + card layout).
 */
export function AuthFlowSkeleton() {
  return <AdminAuthLoadingSkeleton />;
}
