You are the OpenCode executor for Phase B Task 10 only. Do not start Task 11.

Work from: /Users/evolvex/Desktop/Visatop-next-js
Branch: feat/tourist-journey-phase-b

## Hard rules
- Types `T*`, interfaces `I*`, arrow functions only
- TDD
- Do NOT commit / git stash
- One customer total. Never affiliate / fee itemization in JSON or UI
- Webhooks are source of truth. Idempotent. Skip already paid members
- UI never says “party”. Recap: **Traveller 1 — {serviceName}**
- `runtime = "nodejs"` stays on checkout route
- After `resolveApplicationAccess` succeeds, run checkout writes in **`withSystemDbActor`** (sibling rows + `application_party` have no client write). Do not use `withClientDbActor` for the money path.
- Reuse `loadPartyMembers` / `application.partyId`. Legacy null `partyId` = one member (current app)
- `export const runtime = "nodejs"` on any new route

## Task 10: Multi-traveller checkout total + webhook fan-out

### Create `lib/payments/party-checkout-total.ts` + test

```typescript
export type TPartyLine = {
  applicationId: string;
  serviceId: string;
  amountMinor: bigint;
  currency: string;
};

export const sumPartyLines = (lines: TPartyLine[]): bigint =>
  lines.reduce((acc, l) => acc + l.amountMinor, 0n);
```

Also a helper that, given N `CheckoutTotal | null`, returns the summed minor **or** null if any missing / mixed currency.

Tests: 2 lines 12000 + 8000 → 20000n; empty → 0n; mixed currency → treat as error (return null or throw a typed error — checkout maps to 400).

### Checkout `app/api/checkout/route.ts`

If `applicationId` is any member of a party, resolve **all** members (order by travelerIndex). Access already proven for the requested id; cookie hash is on every member.

- `resolveCheckoutTotal` **per member**. Any missing price → rollback checkoutState on locked rows, 400 `details: { reason: "pricing_unavailable" }`
- Sum with `sumPartyLines`. One `price_quote` on **primary**. One `payment` on **primary**. Amount = sum.
- Freeze `checkoutState: pending` on **all** members (same lock predicate as today, applied per member). If any member fails the lock, roll back those already pending and 409.
- `paymentStatus: checkout_created` on all members
- Metadata (Paddle customData is `Record<string, string>` — stringify ids): `applicationId` (primary), `partyId`, `priceQuoteId`, `userId?`, `isGuest`, `serviceId` (primary). No affiliate keys.
- Ziina success/cancel URLs use the **primary** application id
- Solo / legacy: same as today (one line)

On any pricing/FX failure after lock, reset **all** locked members to `checkoutState: none`.

### Webhook `lib/payments/apply-payment-webhook-event.ts`

On `payment_completed` first paid transition:

- Load siblings if `appRow.partyId` set (`loadPartyMembers` or select by partyId)
- For **each** member not already `paymentStatus = paid`: set `paid` / `in_progress` / `fulfillmentStatus: automation_running` / `checkoutState: none`
- `retainRequiredDocuments(tx, memberId)` **per member** (Document rules extras included). If one fails, flag that member `adminAttentionRequired` (do not skip the others)
- `application_party.paymentStatus = paid`
- Skip members already paid
- Still retain on primary as today (covered by the loop — do not retain twice for the same id)

On `payment_failed`: unfreeze `checkoutState` on **all** members.

Add test: two members both paid; `retainRequiredDocuments` called twice (two different ids). Mock tx must support `select` if you query members — or inject a loader.

Existing tests must still pass.

### Recap `components/apply/checkout-order-recap.tsx`

GET application already returns `members`. Thread `members` from draft payment section (or fetch apply GET).

- N>1: list “Traveller 1 — {serviceName}” (and Traveller 2…) with per-line price; **one** total
- `AllInPriceBadges` from apply-config (reuse component + defaults)
- N=1: keep current single product recap (can still show badges)

```bash
pnpm exec vitest run lib/payments/party-checkout-total.test.ts lib/payments/apply-payment-webhook-event.test.ts
```

Print file list. Do not implement Task 11 admin UI.
